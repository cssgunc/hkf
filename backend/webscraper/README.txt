make sure you have python3 working
to install python dependencies, navigate to backend/webscraper and run the following command:
pip install -r requirements.txt
to test, navigate to backend/webscraper and run the following command:
python3 test.py